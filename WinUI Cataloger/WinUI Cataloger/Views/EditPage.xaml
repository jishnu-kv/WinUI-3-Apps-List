<Page
    x:Class="WinUI_Cataloger.Views.EditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI_Cataloger.Views"
    xmlns:labs="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24,24,24,8" RowDefinitions="Auto,*,Auto">
        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16" ColumnDefinitions="*,Auto" RowDefinitions="Auto">
            <TextBlock Grid.Row="0" Text="Batch Edit &amp; Add Apps" Style="{ThemeResource TitleTextBlockStyle}" />
            <Button Grid.Row="0" Grid.Column="1" Style="{ThemeResource AccentButtonStyle}" Click="SaveAllButton_Click" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE74E;" />
                    <TextBlock Text="Save All Changes" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- Main Content Area Frame (hosts AddAppsPage) -->
        <Frame x:Name="ContentFrame" Grid.Row="1" IsNavigationStackEnabled="False" />

        <!-- Status Banner -->
        <InfoBar Grid.Row="2" x:Name="SaveStatusBar" IsOpen="False" Severity="Success" IsClosable="True" Margin="0,16,0,0" />
    </Grid>
</Page>
